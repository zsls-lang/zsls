<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsls.mapper.db2.Test2Mapper">
	<insert id="getsave" parameterType="com.zsls.model.Test2">
		insert into test2(name)
		values(#{name})
	</insert>
    <select id="getInfo" resultType="map">
        select  * from test2
    </select>
</mapper>